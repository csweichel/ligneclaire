# Moore Curve

`moore-curve` renders the closed Moore loop, a Hilbert-derived space-filling curve generated from the classic 90-degree rewrite rules.

## Parameters

- `order`: recursion order of the Moore curve

## Validation Cases

- `default`: a centered Moore loop
