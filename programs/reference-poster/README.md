# Reference Poster

`reference-poster` ports the larger-format contour poster composition from `go-pen`.

## Parameters

- `lines`: base contour lane count
- `spacing`: base lane spacing
- `samples`: spline samples per control span
- `caps`: rounded cap resolution
- `tension`: base spline tension

## Validation Cases

- `default`: the full poster layout
