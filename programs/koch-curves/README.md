# Koch Curves

`koch-curves` renders the Koch line/snowflake family from the standard 60-degree L-system.

## Parameters

- `iterations`: number of L-system expansions
- `snowflake`: closes the curve into the classic three-sided Koch snowflake

## Validation Cases

- `default`: mid-order Koch snowflake
