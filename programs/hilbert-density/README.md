# Hilbert Density

`hilbert-density` ports the density-modulated Hilbert sketch from `go-pen`.

## Parameters

- `order`: Hilbert curve order

## Validation Cases

- `default`: the original dense A4 composition
