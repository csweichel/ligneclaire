# Hello World

`hello-world` ports the mirrored line-fan sketch from `go-pen`.

## Parameters

- `rays`: number of line pairs
- `reach`: horizontal travel of each ray
- `spread`: vertical spread of the fan
- `wobble`: slight deterministic jitter to soften the perfect symmetry

## Validation Cases

- `default`: the standard mirrored fan composition
