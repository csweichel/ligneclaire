# Hilbert Thick Gradient

`hilbert-thick-gradient` ports the landscape Hilbert thickening study from `go-pen`.

## Parameters

- `order`: Hilbert curve order
- `minExtra`: minimum added stroke count
- `maxExtra`: maximum added stroke count
- `step`: spacing between offset lanes

## Validation Cases

- `default`: the default landscape gradient
