# Continuous Contour

`continuous-contour` ports the poster-style contour composition from `go-pen`.

## Parameters

- `lines`: base contour lane count
- `spacing`: lane spacing
- `samples`: spline samples per control span
- `caps`: rounded cap resolution
- `tension`: spline tension

## Validation Cases

- `default`: the reference poster layout
